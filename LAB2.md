# Лабораторная работа №2 — Гайд по выполнению

Чек-лист по заданиям: что уже сделано в репозитории и что нужно сделать самостоятельно.

---

## Задание 1. Изучить основы IaC (инфраструктура как код)

**Что сделать:** Изучить работу с инструментом управления инфраструктурой как кодом (например, Terraform или Ansible).

**Статус:** выполняется самостоятельно (теория + практика в заданиях 2–3).

**Полезно:**
- Документация [Terraform](https://developer.hashicorp.com/terraform/docs) (провайдеры Yandex Cloud, AWS и т.д.).
- Документация [Ansible](https://docs.ansible.com/).

---

## Задание 2. Развернуть ВМ в облаке с помощью Terraform

**Что сделать:** Развернуть виртуальную машину в облаке через Terraform. Установка ПО на ВМ — с помощью конфигурационных файлов (например, cloud-init или последующий Ansible).

**Статус:** нужно сделать самостоятельно.

**Шаги:**
1. Выбрать облако (Yandex Cloud, AWS, Azure и т.д.) и завести аккаунт.
2. Создать папку в репозитории, например `terraform/` или `infra/`.
3. Описать в `.tf` файлах:
   - провайдер облака;
   - сеть/подсеть при необходимости;
   - ресурс ВМ (образ ОС, размер, ключ SSH, cloud-init или user_data).
4. Выполнить `terraform init`, `terraform plan`, `terraform apply`.
5. Проверить доступ по SSH на созданную ВМ.

**Пример структуры (идея, не готовая конфигурация):**
```
infra/
  main.tf       # provider, VM resource
  variables.tf  # переменные (ключ, образ, и т.д.)
  outputs.tf    # output: public_ip ВМ
```

---

## Задание 3. Установить Docker на ВМ с помощью Ansible

**Что сделать:** Установить Docker на виртуальную машину с помощью Ansible (установка — из конфигурационных файлов; если Docker уже был установлен на шаге 2, задание можно считать выполненным). Изучить основы работы с контейнерами.

**Статус:** нужно сделать самостоятельно.

**Шаги:**
1. Установить Ansible на свою машину (`pip install ansible` или пакетным менеджером).
2. Создать папку, например `ansible/`, с плейбуком и инвентори.
3. В плейбуке:
   - указать целевой хост (IP ВМ из Terraform);
   - установить Docker (пакет + сервис, при необходимости пользователь в группу `docker`).
4. Запустить: `ansible-playbook -i inventory.yml playbook.yml`.
5. По SSH зайти на ВМ и проверить: `docker run hello-world`.

**Пример структуры:**
```
ansible/
  inventory.yml   # [hosts], ansible_host=IP, ansible_user=...
  playbook.yml    # tasks: install docker, enable service
```

---

## Задание 4. Dockerfile для server/client, docker-compose, реестр образов

**Что сделать:**  
- Отдельный Dockerfile для серверного приложения.  
- Отдельный Dockerfile для клиентского приложения.  
- `docker-compose.yml` минимум из 3 сервисов: серверное приложение, клиентское приложение, база данных.  
- Собрать образы и сохранить их в облачном реестре Docker-образов.

**Статус:** сделано в репозитории.

### Что уже есть

| Элемент | Где лежит | Кратко |
|--------|-----------|--------|
| Dockerfile сервера | `server/Dockerfile` | Maven → JAR, образ на JRE 21, подключение к PostgreSQL |
| Dockerfile клиента | `client/Dockerfile` | Node → build, nginx раздаёт статику и проксирует `/api` на сервер |
| Конфиг nginx для клиента | `client/nginx.conf` | Проксирование `/api` на `server:3001` |
| docker-compose | `docker-compose.yml` в корне | 3 сервиса: `db` (PostgreSQL), `server`, `client` |
| Инструкции по реестру | README (раздел Docker) | Сборка, теги, push в Docker Hub и ghcr.io |

### Что нужно сделать тебе

1. **Проверить запуск локально**
   ```bash
   docker compose up --build
   ```
   Открыть в браузере: http://localhost

2. **Собрать образы и отправить в облачный реестр** (Docker Hub или GitHub Container Registry), как в README:
   - залогиниться в реестр;
   - сделать `docker compose build`, протегировать образы под свой логин/реестр;
   - выполнить `docker push` для образов `taskmanager-server` и `taskmanager-client`.

3. **Для отчёта/защиты** можно приложить:
   - скрин или вывод `docker images` с твоими образами;
   - ссылки на образы в реестре (например, `docker.io/lesswixx/taskmanager-server:latest` и аналог для client).

---

## Краткая сводка

| № | Задание | Статус |
|---|---------|--------|
| 1 | Основы IaC (Terraform/Ansible) | Самостоятельно (теория + задания 2–3) |
| 2 | ВМ в облаке через Terraform | Сделать: папка с `.tf`, `apply`, доступ по SSH |
| 3 | Docker на ВМ через Ansible | Сделать: плейбук, inventory, проверка `docker run` |
| 4 | Dockerfile server/client + compose + реестр | Готово в репо; осталось запустить и отправить образы в реестр |

После выполнения 2 и 3 можно развернуть приложение из задания 4 на созданной ВМ (запуск через `docker compose` по SSH или копирование файлов на ВМ).
